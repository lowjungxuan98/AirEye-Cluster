apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: kc-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  vaultAuthRef: vault-auth
  mount: secret
  type: kv-v2
  path: aireye-cluster/kc
  refreshAfter: 30s
  destination:
    name: kc-secret
    create: true
    overwrite: true
  rolloutRestartTargets:
    - kind: Deployment
      name: keycloak
