apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: langfuse
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://langfuse.github.io/langfuse-k8s
      chart: langfuse
      targetRevision: 1.5.31
      helm:
        releaseName: langfuse
        values: |
          langfuse:
            web:
              livenessProbe:
                initialDelaySeconds: 120
              readinessProbe:
                initialDelaySeconds: 120
            nextauth:
              url: https://langfuse.lowjungxuan.dpdns.org
              secret:
                secretKeyRef:
                  name: langfuse-secret
                  key: NEXTAUTH_SECRET
            salt:
              secretKeyRef:
                name: langfuse-secret
                key: SALT
            encryptionKey:
              secretKeyRef:
                name: langfuse-secret
                key: ENCRYPTION_KEY
            ingress:
              enabled: true
              className: nginx
              annotations:
                nginx.ingress.kubernetes.io/proxy-body-size: "0"
              hosts:
                - host: langfuse.lowjungxuan.dpdns.org
                  paths:
                    - path: /
                      pathType: Prefix
              tls:
                enabled: true
                secretName: langfuse-tls
            deployment:
              annotations:
                argocd.argoproj.io/sync-wave: "10"
            tolerations:
              - key: node-role.kubernetes.io/control-plane
                effect: NoSchedule
            additionalEnv:
              - name: AUTH_KEYCLOAK_CLIENT_ID
                valueFrom:
                  secretKeyRef:
                    name: langfuse-keycloak-oidc
                    key: AUTH_KEYCLOAK_CLIENT_ID
              - name: AUTH_KEYCLOAK_CLIENT_SECRET
                valueFrom:
                  secretKeyRef:
                    name: langfuse-keycloak-oidc
                    key: AUTH_KEYCLOAK_CLIENT_SECRET
              - name: AUTH_KEYCLOAK_ISSUER
                value: https://keycloak.lowjungxuan.dpdns.org/realms/master
              - name: AUTH_KEYCLOAK_SCOPE
                value: openid email profile
              - name: REDIS_HOST
                value: redis.infra.svc.cluster.local
              - name: REDIS_PORT
                value: "6379"
              - name: LANGFUSE_INIT_ORG_ID
                value: air_eye
              - name: LANGFUSE_INIT_ORG_NAME
                value: air_eye
              - name: LANGFUSE_INIT_PROJECT_ID
                value: litellm
              - name: LANGFUSE_INIT_PROJECT_NAME
                value: LiteLLM
              - name: LANGFUSE_INIT_PROJECT_PUBLIC_KEY
                valueFrom:
                  secretKeyRef:
                    name: langfuse-secret
                    key: LANGFUSE_INIT_PROJECT_PUBLIC_KEY
              - name: LANGFUSE_INIT_PROJECT_SECRET_KEY
                valueFrom:
                  secretKeyRef:
                    name: langfuse-secret
                    key: LANGFUSE_INIT_PROJECT_SECRET_KEY

          postgresql:
            deploy: false
            host: postgres.infra.svc.cluster.local
            port: 5432
            auth:
              username: postgres
              existingSecret: pg-secret
              secretKeys:
                userPasswordKey: POSTGRES_PASSWORD
                adminPasswordKey: POSTGRES_PASSWORD
              database: langfuse

          redis:
            deploy: false
            host: redis.infra.svc.cluster.local
            port: 6379
            auth:
              username: null
              existingSecret: redis-secret
              existingSecretPasswordKey: REDIS_PASSWORD
              database: 0
            cluster:
              enabled: false

          clickhouse:
            deploy: true
            clusterEnabled: false
            replicaCount: 1
            # Disable ClickHouse's internal telemetry tables. On this small,
            # memory-overcommitted node their background merges blow past the
            # ~2.25Gi server-memory cap (Code 241 MEMORY_LIMIT_EXCEEDED), which
            # crashes ClickHouse and cascades into the langfuse-worker nil-pointer
            # panic / CrashLoop. Also bound merge size as a safety net.
            extraOverrides: |
              <clickhouse>
                <asynchronous_metric_log remove="1"/>
                <metric_log remove="1"/>
                <text_log remove="1"/>
                <trace_log remove="1"/>
                <latency_log remove="1"/>
                <query_log remove="1"/>
                <query_thread_log remove="1"/>
                <query_views_log remove="1"/>
                <part_log remove="1"/>
                <processors_profile_log remove="1"/>
                <opentelemetry_span_log remove="1"/>
                <session_log remove="1"/>
                <crash_log remove="1"/>
                <backup_log remove="1"/>
                <blob_storage_log remove="1"/>
                <error_log remove="1"/>
                <merge_tree>
                  <max_bytes_to_merge_at_max_space_in_pool>1073741824</max_bytes_to_merge_at_max_space_in_pool>
                </merge_tree>
              </clickhouse>
            resources:
              requests:
                cpu: 500m
                memory: 1Gi
              limits:
                memory: 2560Mi
            auth:
              username: default
              existingSecret: langfuse-secret
              existingSecretKey: CLICKHOUSE_PASSWORD
            persistence:
              enabled: true
              storageClass: local-path
              size: 20Gi
            tolerations:
              - key: node-role.kubernetes.io/control-plane
                effect: NoSchedule
            zookeeper:
              replicaCount: 1
              persistence:
                enabled: true
                storageClass: local-path
                size: 8Gi
              tolerations:
                - key: node-role.kubernetes.io/control-plane
                  effect: NoSchedule

          s3:
            deploy: false
            bucket: langfuse
            region: us-east-1
            endpoint: http://minio.infra.svc.cluster.local:9000
            forcePathStyle: true
            accessKeyId:
              secretKeyRef:
                name: langfuse-secret
                key: S3_ACCESS_KEY_ID
            secretAccessKey:
              secretKeyRef:
                name: langfuse-secret
                key: S3_SECRET_ACCESS_KEY
    - repoURL: https://github.com/lowjungxuan98/AirEye-Cluster
      targetRevision: main
      path: langfuse
  destination:
    server: https://kubernetes.default.svc
    namespace: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
